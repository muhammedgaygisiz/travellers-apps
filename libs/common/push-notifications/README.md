# push-notifications

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build push-notifications` to build the library.

## Running unit tests

Run `nx test push-notifications` to execute the unit tests via [Jest](https://jestjs.io).
