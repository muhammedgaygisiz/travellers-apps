# bt-admin-restaurants

Restaurant-candidate verification, the unmatched Bite places, and the
new-restaurant form both of them open.

## Running unit tests

Run `nx test bt-admin-restaurants` to execute the unit tests.
