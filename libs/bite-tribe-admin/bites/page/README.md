# bt-admin-bites

The operator's Bite lookup: type a name or a tag, get the Bites that match, and
select one.

Selecting a Bite shows what BiteTribe holds about it. The action an operator
takes on it — deleting an improper Bite — is issue #1475.

## Running unit tests

Run `nx test bt-admin-bites` to execute the unit tests.
