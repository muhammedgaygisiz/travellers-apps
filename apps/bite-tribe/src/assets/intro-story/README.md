# Intro story video assets

Generated by **dev-only** Remotion (`tools/intro-story-remotion`).

```bash
cd tools/intro-story-remotion && npm install && npm run render
```

Do not hand-edit these files — re-render from Remotion after composition changes.

| File            | Beat                           |
| --------------- | ------------------------------ |
| `discover.webm` | Home feed scroll + Create Bite |
| `share.webm`    | Camera + create form           |
| `tribe.webm`    | Bite details / share action    |
| `go.webm`       | Bitemap pins + drawer          |
