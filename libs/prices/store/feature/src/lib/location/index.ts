import { key } from './key';
import { reducer } from './reducer';
import { selectLocation } from './selectors';

const fromLocation = {
  key,
  reducer,
  selectLocation,
};

export { fromLocation };
