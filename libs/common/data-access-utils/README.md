# data-access-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-access-utils` to build the library.

## Running unit tests

Run `nx test data-access-utils` to execute the unit tests via [Jest](https://jestjs.io).
