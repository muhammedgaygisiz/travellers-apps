{
  "name": "distance",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/common/distance/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:common", "framework:angular"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/common/distance"
      }
    }
  }
}
