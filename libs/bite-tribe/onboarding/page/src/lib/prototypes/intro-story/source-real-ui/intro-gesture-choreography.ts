/**
 * @deprecated Import from `../gesture` instead.
 * Compat shim — beat scripts live in the gesture framework.
 */
export {
  INTRO_BEAT_SCRIPTS,
  type IntroBeatScript,
  type IntroStageScreen,
} from '../gesture';
