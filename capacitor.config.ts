import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.travellersapps.prices',
  appName: 'prices',
  webDir: 'dist/apps/prices',
  bundledWebRuntime: false,
};

export default config;
