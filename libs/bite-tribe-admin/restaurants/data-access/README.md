# bt-admin-restaurants-data-access

The candidate and Bite-place reads, and the verification callable behind them.

## Running unit tests

Run `nx test bt-admin-restaurants-data-access` to execute the unit tests.
