# common-ui-qr-code

The QR encoder and its SVG renderer, shared by every surface in this workspace
that prints a code (GitHub issues #1087 and #370).

## Running unit tests

Run `nx test common-ui-qr-code` to execute the unit tests.
