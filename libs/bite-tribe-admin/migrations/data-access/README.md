# bt-admin-migrations-data-access

The Firestore reads, Storage writes and callables behind the operational
migrations.

## Running unit tests

Run `nx test bt-admin-migrations-data-access` to execute the unit tests.
