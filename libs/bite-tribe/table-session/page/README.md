# table-session

The guest-facing half of a scanned table QR code (GitHub issue #1101): the
confirmation screen that names the restaurant and the table, and the twelve
sentences a refused scan gets instead.

## Running unit tests

Run `nx test table-session` to execute the unit tests.
