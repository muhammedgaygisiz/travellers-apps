import { key } from './key';
import { reducer } from './reducer';
import { bucketlists } from './selectors';

const fromBucketlists = {
  key,
  reducer,
  bucketlists,
};

export { fromBucketlists };
